import type { DiagramMenuConfig } from "../../../types/menu/DiagramMenuConfig";
import { createMenuConfig } from "../core/createMenuConfig";

/**
 * Menu configuration for PathPoint shapes.
 * Only stack order is enabled (no features defined).
 */
export const PathPointMenuConfig: DiagramMenuConfig = createMenuConfig({});

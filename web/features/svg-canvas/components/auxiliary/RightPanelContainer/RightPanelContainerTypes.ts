import type { ReactNode } from "react";

/**
 * Props for the RightPanelContainer component.
 */
export type RightPanelContainerProps = {
	/** Children components to render in the container */
	children: ReactNode;
};

/**
 * Props for the BottomPanelContainer component.
 */
export type BottomPanelContainerProps = {
	/** Children components to render in the container */
	children: ReactNode;
};

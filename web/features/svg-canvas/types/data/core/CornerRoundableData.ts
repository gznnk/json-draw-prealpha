/**
 * Interface for diagram elements that can have corner radius.
 * Provides the cornerRadius property for elements that can be rounded.
 */
export type CornerRoundableData = {
	cornerRadius: number;
};

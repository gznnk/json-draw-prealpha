/**
 * Type for grab scroll state in the SVG canvas.
 * Manages the state of grab scrolling functionality.
 */
export type GrabScrollState = {
	/** Whether the user is currently grabbing (actively dragging) */
	isGrabbing: boolean;
};

/**
 * Max history size for the history stack.
 */
export const MAX_HISTORY_SIZE = 20;

/**
 * The amount of space to expand the canvas when a shape is dragged near the edge.
 */
export const CANVAS_EXPANSION_SIZE = 300;
